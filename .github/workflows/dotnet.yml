# # This workflow will build a .NET project
# # For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

# name: .NET

# on:
#   push:
#     branches: [ "master" ]
#   pull_request:
#     branches: [ "master" ]

# jobs:
#   build:

#     runs-on: windows-latest

#     steps:
#     - uses: actions/checkout@v4
#     - name: Setup .NET
#       uses: actions/setup-dotnet@v4
#       with:
#         dotnet-version: 8.0.x
#     - name: Restore dependencies
#       run: dotnet restore --no-cache
#     - name: Build
#       run: dotnet build --no-restore --configuration Release --framework net8.0
#     - name: Test
#       run: dotnet test --no-build --verbosity normal --configuration Release --framework net8.0

name: Build Selected .NET Projects

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET 8.0
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    # 방법 1: 특정 프로젝트 파일들을 직접 지정하여 빌드
    - name: Build .NET 8.0 Projects (Method 1 - Specific Projects)
      run: |
        dotnet build MyCommonNet/MyCommonNet.csproj --configuration Release
        dotnet build NetCoreWebServer/NetCoreWebServer.csproj --configuration Release
        dotnet build TcpServerStandard/TcpServerStandard.csproj --configuration Release
        dotnet build TestTcpClient/TestTcpClient.csproj --configuration Release
        dotnet build NetStandardUnitTest/NetStandardUnitTest.csproj --configuration Release
        
        # 추가 .NET 8.0 프로젝트들을 여기에 나열